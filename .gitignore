# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
# .env* で全部落とし、コミットしてよいテンプレートだけ戻す。
# 以前は .env*.local だけだったため、.env / .env.production などに
# 実キーを書くとコミットされる状態だった。
.env*
!.env.local.example

# Common ignores
node_modules
.next/
.DS_Store
.vercel

# Build artifacts
*.tsbuildinfo

# Claude Code のローカル個人設定（許可リスト等）。deployment.md は共有する
.claude/settings.local.json
